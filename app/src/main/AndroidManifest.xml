<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportprokg">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/SplashTheme">
        <activity android:name=".CompetitionsFilterTypeActivity"></activity>
        <activity android:name=".CompetitionsFilterActivity" />
        <activity android:name=".ui.Activities.NewsFilterTypeActivity" />
        <activity android:name=".ui.Activities.NewsFilterActivity" />
        <activity android:name=".ui.Activities.CreateSportsmanActivity" />
        <activity android:name=".ui.Activities.SportsmanActivity" />
        <activity android:name=".ui.fragments.referee.RefereeJudgeActivity" />
        <activity android:name=".ui.fragments.referee.RefereeChooseCategoryActivity" />
        <activity android:name=".ui.Activities.RefereeCompetitionActivity" />
        <activity android:name=".ui.Activities.DetailedCompetitionsActivity" />
        <activity android:name=".ui.Activities.DetailedNewsActivity" />
        <activity android:name=".ui.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>